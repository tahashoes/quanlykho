{
  "name": "quan-ly-kho",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Ứng dụng quản lý hàng hóa cho cửa hàng nhỏ và vừa.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check report-export.js && node --check public/app.js",
    "test": "node --test test/integration.test.js"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "exceljs": "4.4.0",
    "pdfkit": "0.19.1"
  },
  "overrides": {
    "uuid": "11.1.1"
  }
}
